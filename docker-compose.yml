version: '3.8'

services:
  realfont:
    build: .
    container_name: realfont-app
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
    networks:
      - realfont-network

networks:
  realfont-network:
    driver: bridge
